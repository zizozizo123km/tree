هذه هي الوثيقة التقنية والهندسية الكاملة لمشروع "موقع فيسبوك html"، وهي مصممة لتكون مرجعاً شاملاً للمطورين والمهندسين لضمان استمرارية الجودة والأداء العالي.

---

# 🏗️ وثيقة الهندسة المعمارية للمشروع (Architecture Documentation)

## 1. نظرة عامة على المشروع (Project Overview)
يهدف المشروع إلى بناء واجهة مستخدم متقدمة وشاملة لمحاكاة منصة التواصل الاجتماعي "فيسبوك"، مع التركيز على الأداء العالي (High Performance)، التصميم العصري (Modern Industrial Design)، واستخدام تقنيات الويب الحديثة بدون الحاجة إلى أطر عمل ثقيلة، اعتماداً على مبدأ **Vanilla JS Modular Architecture**.

---

## 2. الهيكل التنظيمي للملفات (Project Structure)

تم تنظيم المشروع لضمان فصل الاهتمامات (Separation of Concerns) وسهولة الصيانة:

```text
├── index.html                # الصفحة الرئيسية (التغذية الإخبارية)
├── login.html                # صفحة تسجيل الدخول
├── register.html             # صفحة إنشاء حساب جديد
├── profile.html              # الصفحة الشخصية للمستخدم
├── messages.html             # منصة المراسلة الفورية
├── notifications.html        # مركز التنبيهات
├── friends.html              # إدارة الأصدقاء والطلبات
├── marketplace.html          # المتجر ومنصة البيع
├── groups.html               # المجموعات والمجتمعات
├── settings.html             # إعدادات الحساب والخصوصية
├── js/
│   ├── core/
│   │   └── app.js            # المحرك الرئيسي وربط الوحدات
│   ├── modules/
│   │   ├── auth.js           # منطق المصادقة والتحقق
│   │   ├── feed.js           # إدارة المنشورات والتفاعل
│   │   ├── messaging.js      # معالجة الرسائل والدردشة
│   │   └── ui-controller.js  # التحكم في العناصر التفاعلية والتحريك
│   └── utils/
│       └── api-mock.js       # محاكاة البيانات وطلبات الـ API
├── assets/
│   ├── images/
│   │   ├── brand/            # الشعارات والهوية البصرية
│   │   └── ui/               # الصور الافتراضية والرموز
│   └── icons/
│       └── icon-sprite.svg   # مجمع الأيقونات عالي الأداء
└── docs/
    └── architecture.md       # هذه الوثيقة
```

---

## 3. المكدس التقني (Technical Stack)

*   **Frontend:** HTML5, Tailwind CSS (via CDN).
*   **Custom Styling:** CSS3 (Internal Styles) للرسوم المتحركة المتقدمة وتأثيرات الـ Glassmorphism.
*   **Scripting:** Vanilla JavaScript (ES6+ Modules).
*   **Data Handling:** JSON-based Mock API لعمليات الـ CRUD.
*   **Icons:** SVG Sprites لضمان سرعة التحميل.

---

## 4. التصميم الهندسي للنظام (System Design)

### أ. طبقة الواجهة (UI Layer)
تعتمد على نظام **Atomic Design**؛ حيث يتم تقسيم الواجهة إلى:
- **Elements:** (الأزرار، الحقول، الأيقونات).
- **Components:** (بطاقة المنشور، شريط التنقل، قائمة الأصدقاء).
- **Layouts:** (نظام الشبكة الثلاثي: الجانب الأيمن للملاحة، الوسط للمحتوى، اليسار للتواصل).

### ب. طبقة المنطق (Logic Layer)
تستخدم نمط **Module Pattern**:
- كل ملف في `js/modules/` يعمل بشكل مستقل (Encapsulated).
- يتم التواصل بين الوحدات عبر `app.js` لضمان عدم حدوث تداخل في الذاكرة.

### ج. محاكاة البيانات (Data Abstraction)
يتم استخدام `api-mock.js` كطبقة وسيطة (Interceptor). بدلاً من استدعاء `fetch` مباشرة، تقوم الوحدات بطلب البيانات من الـ Mock، مما يسهل عملية الانتقال إلى API حقيقي مستقبلاً.

---

## 5. قواعد التصميم والهوية البصرية (Design System)

| العنصر | المواصفات |
| :--- | :--- |
| **الألوان الأساسية** | `#1877F2` (الأزرق الرسمي)، `#F0F2F5` (الخلفية)، `#FFFFFF` (البطاقات) |
| **الوضع الليلي** | دعم كامل عبر فئات `dark:` في Tailwind |
| **الخطوط** | Tajawal (للعربية)، Segoe UI (للنظام) |
| **التحريك** | استخدام `Cubic-bezier` للانتقالات السلسة |

---

## 6. تدفق البيانات (Data Flow)

1.  **حدث المستخدم:** يضغط المستخدم على "إعجاب".
2.  **UI Controller:** يلتقط الحدث ويقوم بتحديث الواجهة فوراً (Optimistic UI).
3.  **Feed Module:** يرسل طلباً إلى `api-mock.js` لتحديث الحالة في "قاعدة البيانات الوهمية".
4.  **Sync:** في حال فشل الطلب، يقوم النظام بعمل Rollback للحالة في الواجهة.

---

## 7. معايير الأداء والأمان (Performance & Security)

*   **Lazy Loading:** يتم تحميل الصور والوحدات البرمجية فقط عند الحاجة.
*   **DOM Sanitization:** يتم تنظيف أي مدخلات مستخدم قبل عرضها لمنع هجمات XSS.
*   **Asset Optimization:** استخدام SVG بدلاً من الصور النقطية للأيقونات لتقليل حجم الصفحة.

---

## 8. دليل المطور (Developer Guidelines)

*   **تسمية الفئات:** اتبع معيار BEM عند كتابة CSS مخصص داخل الـ `<style>`.
*   **التعليقات:** يجب توثيق كل دالة برمجية باستخدام JSDoc.
*   **التجاوب:** يجب أن تعمل جميع الصفحات بكفاءة على شاشات تبدأ من 320px وحتى 4K.

---

**ملاحظة:** تم تصميم هذه المعمارية لتكون قابلة للتوسع (Scalable)، بحيث يمكن تحويل المشروع إلى تطبيق Single Page Application (SPA) بسهولة باستخدام React أو Vue في المستقبل دون تغيير الهيكل الأساسي للبيانات.